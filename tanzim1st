cosnt express = require('express');
cobst path = require('path');

const app = express();

app.use(express.json());
app.use(express.urlencoded({ extended: false }));

app.get('/', (reg, res) =>
  res.send('hello from express');
  });
  
  app.lsiten(5000, {} => console.log('Server started on 5000'));
  
